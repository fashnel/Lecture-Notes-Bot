services:
  lecture-worker:
    build: .
    container_name: lecture-notes-bot
    env_file:
      - .env
    volumes:
      - ./data:/data
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1.0G
          cpus: "1.5"
